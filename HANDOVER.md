# HANDOVER — передача состояния конвейера

Живой журнал состояния. **Читать при старте сессии. Дописывать в конце работы.**
Здесь: что сделано по этапам, какие настройки приняты и почему, что не работает,
следующий шаг. Все настройки — в `presets/*.json`, не в коде.

Обновлено: 2026-08-13.

---

## Порядок этапов (scripts/loop.py → STAGES)

Сначала ВСЯ обработка на длинном ГОРИЗОНТАЛЬНОМ видео, потом Vizard режет
вертикаль 9:16, и ТОЛЬКО ПОТОМ субтитры (иначе Vizard обрежет бока и текст поедет):

`00-plan → 01-script → 02a-dupes → 02b-audio → 02c-stab → 03-color → 03a-cut → 03b-subtitles → 04-caption → 05-qa → 06-publish`

Проверка каждого этапа — кнопкой в Actions (`*-test.yml`), скачивание с YouTube
через секрет `YT_COOKIES` (работает, куки живые — не трогать).

---

## СКВОЗНОЙ ПРОГОН — состояние сцепки этапов (2026-08-12)

Первый сквозной прогон (`run-through.yml`, run 31608197906, LjkSHpRdaPk).
**Автосцепки этапов в коде НЕТ:** `loop.py` — заглушка, реально этапы не
запускает (двигает закладку и печатает отчёт). Каждый скрипт этапа работает сам
по себе; сшивал их драйвер `run-through.yml`.

**Этапы 1→4 сцепляются и проходят целиком** (имена файлов совпадают:
`clip.mp4 → clip_audio_clean.mp4 → clip_stab.mp4 → clip_color.mp4`):
- скачать 39 с, звук 13 с, стабилизация 45 с, цвет 134 с (на 69 с видео; +~6 мин установка).
- Звук пережил цепочку (opus→aac, сохранён до конца). Разрешение 1440×1080 сохранено.
- Стабилизация померила тряску и пропустила (файл побайтово равен входу) — штатно.
- LjkSHpRdaPk оказался ≈69 с (запрошенные 180 не набрались) — «3 минуты» тут нет.

**Оба разрыва закрыты по ВАРИАНТУ A** (решение Анны; `cut-a.yml`, run 31688775588):
1. **Нарезка (4→5) — РЕШЕНО.** `clip_color.mp4` выкладываем временным релиз-ассетом
   (публичный URL), Vizard режет его как `videoType=1` (`scripts/vizard_cut_file.py`),
   субтитры Vizard выкл, клипы качаем по папкам `clip_NN/`, ассет удаляем сразу
   (с проверкой HTTP-кода: 204/404=ок, иначе громкая ошибка). Обработка не теряется.
   - Важно: в режиме нарезки Vizard параметры субтитров недоступны (`subtitleSwitch`
     только для editing-mode). Проверено кадрами сырых клипов — Vizard свой текст
     НЕ вшил, двойных субтитров нет. Если когда-то появятся — смотреть кадры
     `frame_raw_*.jpg`, которые снимает `cut-a.yml` до наших субтитров.
2. **Субтитры (5→6) — РЕШЕНО.** `subtitles.py --clips-dir <папка> --transcribe` —
   проходит по всем клипам, каждый перераспознаёт заново (find_dupes.py) → пересчёт
   таймингов под клип. Одиночный `--input` не изменён.

Прогон run 31688775588 (LjkSHpRdaPk, 69 с): цвет 152 с, релиз-ассет 3 с, Vizard 214 с
(вернул 1 клип, виральность 10, 42 с — ролик короткий, потому 1), субтитры 62 с.
Клип 1080×1920, звук+цвет наши, свои субтитры с жёлтой подсветкой. Публикации нет.

`run-through.yml` и `cut-a.yml` пока с push-триггером по своему файлу (чтобы гонять
до попадания на main) — убрать после стабилизации.

---

## Что сделано по этапам

### 02a-dupes — поиск дублей / расшифровка (`scripts/find_dupes.py`, `presets/dupes.json`)
- Whisper (faster-whisper, medium, int8, ru, VAD) с пословными метками.
- Пишет расшифровку: `transcript.srt`, `transcript.txt`, `transcript.json`
  (пословно — переиспользуется субтитрами, второй раз не распознаём).
- Детектор дублей: фраза, сказанная дважды ПОДРЯД слово в слово, ≥ `prefix_words`
  слов; режет зону перезапусков целиком. Проверено на реальном ролике: ловит
  запинки, риторические приёмы (анафору) не трогает.
- **РЕЗКА ВЫКЛЮЧЕНА** (`cut: "off"`): длинные куски Анна вырезает вручную перед
  заливкой. Этап теперь только распознаёт и даёт `transcript.json`.

### 02b-audio — звук (`scripts/audio_clean.py`, `presets/audio.json`)
- **Цепочка из 4 шагов** (решение Анны): **DeepFilterNet** → **подмес исходника**
  (`amix` 85% очищенного / 15% исходного, `normalize=0`) →
  **`equalizer f=4000:width_type=o:width=1.5:g=4`** → `loudnorm I=-14:LRA=11:TP=-1`.
  Долю исходника держит `presets/audio.json` поле `dry_mix` (умолч. 0.15, клампится
  0..1). Никаких bass/treble/afftdn/arnndn.
- **Почему так:** DeepFilterNet в одиночку перестарался — фон снимался полностью
  («тихий подвал», неживо), верх голоса подрезался («как из трубы»). Подмес 15%
  исходника возвращает воздух комнаты, узкий +4 дБ на 4 кГц — верх голоса.
  Шум убирает **только** DeepFilterNet; equalizer тут не шумодав, а возврат верха.
- **Почему DeepFilterNet:** прошли путь — loudnorm поднимал фон («бочка»); вырез
  полос/полки убивали голос («колодец»); arnndn (это и есть RNNoise, 5 моделей в
  `presets/rnnoise/`) обучен на телефонном качестве — тоже «колодец». DeepFilterNet
  (Rikorose, MIT) — нейросеть-шумодав РЕЧИ, обучена на речи в реальном шуме, сама
  разбирает любой фон. CPU, быстро.
- **Модель в репозитории** (`presets/deepfilternet/DeepFilterNet3/` — config.ini +
  checkpoint, ~8 МБ), скачивать при прогоне не нужно (иначе 403 через прокси).
  Источник и версии — `presets/deepfilternet/SOURCE.md`.
- **Зависимости строго:** `deepfilternet==0.5.6`, `torch==2.0.1`, `torchaudio==2.0.2`.
  Свежий torchaudio убрал `torchaudio.backend` → импорт падает. Ставит `audio-fix.yml`
  (~60 с). deepfilternet сам torch НЕ тянет — ставить отдельно, с CPU-индекса.
- **Проверено на реальном голосе Анны** (https://youtu.be/LjkSHpRdaPk, 30 с, прогон
  audio-fix.yml, run 31605583510). Три точки — исходник → после DFN → итог:
  фон в тихом месте −20.3 → −73.6 → **−25.3 dB** (воздух вернулся, не «подвал»);
  верх 4–8 кГц к общему −28.0 → −22.9 → **−21.4 dB** (эквалайзер поднял верх);
  голос исходника −16.3 LUFS; итог **−15.1 LUFS** (цель −14), пик **−1.0 dBTP**;
  DeepFilterNet считал **1.6 с** на 30 с звука. Файл отдан Анне на послушать.
- Работает через `deepFilter` (не ffmpeg-фильтр): извлекаем wav 48 кГц (он же
  исходник для подмеса) → `deepFilter -m …DeepFilterNet3` → одним `filter_complex`
  `amix` (очищенный + исходник) → `equalizer 4000` → `loudnorm` и возврат дорожки
  в mp4 (видео копия). Замер полосы 4–8 кГц — `band_ratio()` (RMS полосы к общему).

### 02c-stab — стабилизация (`scripts/stabilize.py`, `presets/stab.json`)
- vidstab 2 прохода. Меряет тряску, пропускает если < `skip_below_px` (3.0).
- На реальном материале тряска ~0.2px → этап пропускается. Оставлен как страховка.

### 03-color — цвет и кожа (`scripts/color.py`, `presets/color.json`)
- Замер содержимого кадра через ffmpeg signalstats (не заголовок файла).
- Цвет: LUT `presets/cinema.cube` если есть, иначе авто-eq от замеров.
- Кожа: OpenCV, Haar-каскад находит лицо, сглаживание bilateralFilter ТОЛЬКО по
  коже (глаза/брови/волосы/фон не трогает), уровень medium. + лёгкий unsharp.
- **Принято** («картинка теплее, лицо в норме, кожа живая»).

### 03a-cut — нарезка вертикали (`stages/03a-cut/PROMPT.md`, скилл using-vizard-api)
- Vizard режет вертикаль 9:16 со слежением за лицом.
- **Субтитры Vizard ВЫКЛЮЧЕНЫ** (`subtitleSwitch: 0`) — свои рисуем в 03b.
- ⚠️ Ещё НЕ прогонялся сквозняком (см. «Что не работает»).

### 03b-subtitles — субтитры в кадре (`scripts/subtitles.py`, `presets/subtitles.json`)
- Вжигает субтитры (ASS/libass) в вертикальный кусок. По 3–5 слов на экран, по
  пословным меткам из `transcript.json`.
- Белый текст, чёрная обводка, внизу по центру, крупно.
- **Подсветка**: строка белая, звучащее сейчас слово — жёлтым, переезжает по
  словам (караоке). Жёлтое сохраняет чёрную обводку — читается на любом фоне.
- **Принято** («субтитры вижу, всё хорошо»).

### Остальное
- 00-plan, 01-script, 04-caption, 05-qa, 06-publish — каркас/PROMPT.md, детально
  ещё не отрабатывались в этой линии работы.

---

## Принятые настройки и почему

| Где | Настройка | Значение | Почему |
|---|---|---|---|
| dupes.json | `cut` | **off** | Длинные куски Анна режет сама; авто-резка отключена по её решению |
| dupes.json | `prefix_words` | 3 | Ловит короткие повторы, приёмы не трогает (проверено на 13 местах) |
| audio.json | `target_lufs` / `true_peak_db` | **-9.3 / -1** | Как у эталона Анны (громко, соцсеть) |
| audio.json | `denoise_strength` | **gentle** (afftdn=nr=3) | Самый мягкий — верх не срезаем |
| audio.json | (компрессия/лимитер/деэссер/deboom/highpass/резка пауз) | **убрано** | Анна: только шумодав + громкость, две строки |
| stab.json | `skip_below_px` | 3.0 | Тряски почти нет — не гоняем зря |
| color.json | насыщенность | потолок ×1.3 | Исходник тусклый; авто тянет, но не «кислотно» |
| color.json | кожа | OpenCV medium, только лицо | ffmpeg мылил весь кадр; нужно только кожу |
| subtitles.json | `font_size_pct` | 7.5 (от МЕНЬШЕЙ стороны) | На 9:16 это ширина → текст узкий, не 144px по высоте |
| subtitles.json | `words_per_screen` | 4 | 3–5 слов, читается с телефона |
| subtitles.json | `highlight` / `highlight_color` | on / FFFF00 | Жёлтая подсветка звучащего слова |
| vizard (SKILL.md) | `subtitleSwitch` | 0 | Свои субтитры в нашем стиле, с подсветкой |

Постоянные правила от Анны: смотрит с телефона (размеры с запасом); отчёт
цифрами; результат не выдумывать; куки и Vizard-ключ не трогать; таймеров и
отложенных самопроверок не ставить.

---

## Что не работает / отложено

0. **Скилл `transcript-fixer` (daymade/claude-code-skills) — НЕ СТАВИМ.** Решение
   Анны, не возвращаться. Причина: шлёт текст расшифровки во внешний сервис **GLM**
   (bigmodel) и **читает ключи из окружения** (`GLM_API_KEY`, `ANTHROPIC_API_KEY`);
   плюс сам ставит зависимости через `uv`/`curl|sh`, тянет 106 файлов со своей БД и
   веб-дашбордом. Для конвейера лишнее и риск по секретам. `video-comparer` из того
   же репо — наоборот, поставлен (чистый: stdlib + ffmpeg, без сети и ключей).

1. **Звук — упрощён до двух фильтров, ждём вердикт Анны с телефона.** Сложная
   цепочка (компрессия/лимитер/деэссер/deboom) давала неестественный, глухой,
   звенящий звук — Анна отвергла. Теперь ровно: `afftdn` (мягкий) → `loudnorm -9.3`.
   Отправлен один файл 30 с на послушать. **Открытый вопрос — яркость:** эталон −31,
   из двух фильтров достижимо ~−37 (потолок этой записи −35.7 без шумодава вообще).
   Если Анна захочет ближе к −31 — нужен третий шаг (лёгкий подъём верха), но она
   просила ровно два. Ждём: устраивает −37 или добавляем подъём верха.
2. **Отложенный хвост по звуку:** пик получается −0.9 (потолок −1); эталон 0.1 —
   выше нельзя без клиппинга. LRA после loudnorm сплющивается до ~0.8 (плата за
   громкость −9.3: чтобы поднять на +6 дБ и не выйти за пик, динамику неизбежно
   поджимает — так и делают громкие соцсеть-мастера).
3. **Vizard 03a-cut не прогонялся сквозняком.** Нужно: (а) секрет
   `VIZARDAI_API_KEY`; (б) Vizard должен резать ОБРАБОТАННЫЙ локальный файл
   `clip_color.mp4` (загрузка файла), а не ссылку YouTube — вызов Vizard под это
   ещё не переделан.
4. **transcript.json → куски.** После нарезки на несколько вертикальных кусков
   времена `transcript.json` (целого ролика) надо сопоставлять с таймлайном каждого
   куска, иначе субтитры не сойдутся. Не реализовано.
5. **Сквозного прогона (все этапы подряд) не было** — каждый этап проверялся
   отдельной кнопкой `*-test.yml`.
6. **Надёжное скачивание целого ролика:** хак `--download-sections *0-999999` в тесте
   нестабилен (обрывается). Для теста берём короткий отрезок. Отдельная задача.
7. **Whisper пляшет** от запуска к запуску (распознаёт чуть иначе) — влияет на
   качество расшифровки/субтитров.

---

## Следующий шаг

- Ближайшее: Анна обрезает следующий ролик сама и заливает — продолжаем с него.
- Когда дойдём до сквозной сборки: переделать вызов Vizard на обработанный файл
  (п.3) и привязку `transcript.json` к кускам (п.4).
- Если Анна вернётся к звуку — выбрать вариант из `audio-fix` и закрепить
  deesser/deboom в `audio.json` (п.1).

---

## Как мы работаем (для новой сессии)

- Ветка разработки: `claude/video-pipeline-scaffold-gacj3u`. После каждого влитого
  PR перезапускать ветку от свежего main.
- Цикл: собрал → прогнал кнопкой `*-test.yml` в Actions → показал Анне результат
  ЦИФРАМИ и файлом → она смотрит с телефона → говорит да/что править → влила PR.
- Все видимые Анне тексты — по-русски.
- Кнопки-проверки: `audio-ab`, `audio-test`, `audio-fix`, `stab-test`,
  `color-test`, `dupes-test`, `subs-test`.

---

## Журнал сессий (дописывать в конце работы, новые записи сверху)

### 2026-08-15 (новый этап 03c-inserts «вставки» + цвет + замеры Vizard)
- **Новый этап 03c-inserts — ВСТАВКИ** (главное, чего не хватало для динамики; в
  образце Анны ~10 вставок/мин, все — карточка сверху кадра, лицо не закрыто,
  приближений кадра нет). Создано: `stages/03c-inserts/PROMPT.md`,
  `scripts/inserts.py`, `presets/inserts.json`, папка `assets/inserts/` (+.gitkeep).
  Вписан в `STAGES` (loop.py) сразу после `03b-subtitles`.
  - Ищет ФРАЗЫ-связки целиком по пословному `transcript.json` (переиспользуем с
    этапа субтитров, второй раз не распознаём). Вставка — со следующего слова
    после фразы. Фразы нет — пропуск молча, этап не блокируется.
  - Три вида: **card** (картинка/видео в углу, до трети ширины), **fullscreen**
    (на весь экран на N секунд), **text** (плашка; рисуется через libass — в этом
    ffmpeg нет drawtext). Всё в один проход ffmpeg, звук копией потока.
  - Источник картинок: свой `file` из assets/inserts/ ИЛИ `search` → **Pexels**
    (бесплатно, но нужен ключ). Кэш скачанного — assets/inserts/_cache/ (в .gitignore).
  - Проверено локально: dry-run (поиск фраз по меткам) и реальный рендер
    (карточка сверху + текстовая плашка) — работает, кадры смотрел.
  - **НУЖНО от Анны:** (1) ключ `PEXELS_API_KEY` — https://www.pexels.com/api/
    (бесплатный) → в GitHub Secrets; (2) реальный список фраз/файлов и тексты в
    `presets/inserts.json` (сейчас там её 4 примера-фразы с плейсхолдерами:
    study.png/book.png ещё нет, «цифры говорят» → текст «ЗАМЕНИ НА ЦИФРУ»).
- **Цвет (presets/color.json + color.py):** по просьбе Анны (её клип ярче образца
  113 против 69 и бледнее 11.8 против 18.7). `target_brightness` 120→**69**,
  `target_saturation` 120→**180**; в `color.py` поднят потолок насыщенности
  1.3→**1.6** (при старом потолке добавить до ×1.6 было нельзя). Ждём проверку на
  реальном клипе (цифрами).
- **Замеры по Vizard (прошлый раунд, для памяти):** у Vizard в API НЕТ ручки
  разрешения/качества — исходное качество не попросить. Мыло — от растяжки узкого
  окна 9:16 (из 1440 шир. это 608 px → апскейл ×1.78). Наш `vertical_cut.py`
  мылит так же (58→12), как Vizard (130→33) — рычаг не в резчике, а в разрешении
  исходника (нужен ≥1920 по высоте). Звук: план Анны (копия потока из clip_color
  по меткам, без пересжатия) верный; проверить, что кусок Vizard непрерывный.

### 2026-08-11 (порядок в правилах + режим 3 вариантов звука)
- **Правила разнесены по ролям файлов:** brain/RULES.md — ВСЕ правила поведения
  (1–21 + Ключи/Продающие/Проверка ссылок/Тестовый режим); CLAUDE.md — только
  устройство (этапы, папки, порядок чтения) + ссылка на RULES.md; HANDOVER.md —
  только состояние. Из CLAUDE.md перенесены: раздел «Правила» (латиница, пути, две
  попытки, шаг сделал→проверил→доложил), «Автопроверки», «Когда какой скилл»,
  «Еженедельные темы», «Формат текстов». Убраны дубли (упрощать, по-русски, ключи,
  «что читать при старте», HANDOVER-в-конце сведён к правилу 4). **Новые правила —
  только в RULES.md.**
- Добавлены правила: 19 «Субагенты для параллельных задач», 4 «Память между
  сессиями» (слита со старой «Передача между чатами»). RULES.md версия 7.
- Звук: добавлен режим `audio_clean.py --variants` — три варианта (как сейчас/
  средне/максимум) с фоном по шагам; вырез поддерживает несколько полос. Прогон на
  эталоне: фон итога −25.0 / −25.7 / −27.8 dB (максимум давит на −9.5 к исходнику),
  но и тише: −14.8 / −15.8 / −18.9 LUFS (фикс-гейн не поднимает из-за пика). Ждём
  выбор Анны по шуму+голосу; громкость до −9.3 — отдельно лимитером пиков.
- Удалён мёртвый `scripts/audio_ab.py` + `audio-ab.yml` (импортировали удалённые
  фильтры). `audio-fix.yml`: вход `variants` (true → 3 файла).

### 2026-08-11 (звук: цепочка против «бочки» — highpass+вырез+шумодав+фикс-гейн)
- По решению Анны `audio_clean.py` переписан на 4 шага (чистим → громкость):
  1) highpass 90 Гц (срез низов), 2) вырез 200–400 Гц (`equalizer`, центр 300, −6 дБ,
  убирает гулкость), 3) afftdn (лёгкий, gentle — остаточный шип), 4) громкость
  **лёгким фиксированным гейном** (`volume`), НЕ динамическим loudnorm (тот поднимал
  фон +5.7 дБ). Гейн = min(до цели −9.3, до потолка пика) с запасом `PEAK_HEADROOM_DB`.
- Фон меряется на КАЖДОМ шаге в одном окне (анализ с префиксом фильтров, без
  промежуточных файлов); итог — один проход ffmpeg (одно сжатие AAC, без потерь).
- Все правки по code-review внесены: контроль пика (запас + предупреждение при
  превышении), `float()`-приведение числовых настроек, убраны 4 пересжатия AAC.
- presets/audio.json: `highpass_hz`, `deboom_hz/q/gain`, `denoise_strength gentle`.
- Правила: RULES.md +ПРАВИЛО 12 (не трогать без просьбы: куки, Vizard, принятые
  presets) и +ПРАВИЛО 13 (никаких таймеров). CLAUDE.md: code-review теперь перед
  открытием PR (а не перед каждым коммитом).
- ⚠️ **`scripts/audio_ab.py` сломан (давно, не от этой правки):** импортирует
  несуществующие `COMPAND/HIGHPASS/LIMITER` и `STRENGTH['high']` из audio_clean —
  `ImportError`. A/B-воркфлоу `audio-ab.yml` на нём упадёт. Чинить отдельно, если
  понадобится (сейчас вне задачи).

### 2026-08-11 (звук: замер фона + ключевое открытие про «бочку»)
- `audio_clean.py`: замер фона переведён с `silencedetect` (давал 0 пауз на
  реальном ролике) на прямой RMS по окнам ~0.5 с (`astats`), фон меряется в одном
  и том же окне у исходника/после шумодава/итога. Порядок: шумодав → громкость.
  Правки по code-review (не падать на `-inf`, окно от частоты файла, нет ложного
  предупреждения при `off`).
- **ГЛАВНОЕ (прогон #6 на эталоне https://youtu.be/wXXP9Bzh5bg, 30 с):**
  - фон в тихом месте: исходник **−18.3 dB** (высокий — комната гулкая);
  - после afftdn **strong** (nr=24): **−18.6 dB** — упал всего на **0.3 dB**.
    → Вывод: это НЕ широкополосный шум, afftdn его не берёт. Усиление шумодава
    (по шагу Анны «усиль, если не упал») проверено цифрами — не помогает.
  - после громкости (loudnorm): **−12.9 dB** — фон ПОДНЯЛСЯ на **+5.4 dB**.
    → Ровно «бочка», которую слышит Анна: loudnorm (динамический режим)
    подтягивает тихие места вверх вместе с фоном, даже при порядке «шум→громкость».
- **Следующий шаг (ждём «да» Анны):** afftdn заменить/дополнить на срез низов
  (highpass ~80–100 Гц) + вырез гулкой полосы ~200–400 Гц ДО громкости; и убрать
  подъём фона громкостью (исходник уже ≈−9 LUFS — хватит лёгкого фикс-гейна вместо
  динамического loudnorm с сжатием LRA). Это возврат к highpass/deboom, которые
  раньше убрали в «ровно две вещи» — но цифры показывают, что для гулкой комнаты
  они нужны.
- CLAUDE.md: добавлен раздел 6 «Автоматические проверки». Поставлен скилл
  `.claude/skills/video-comparer` (чистый: stdlib+ffmpeg). `transcript-fixer` НЕ
  ставил — внешний AI-API (GLM) + читает `GLM_API_KEY`/`ANTHROPIC_API_KEY`.

### 2026-08-11 (документация и хвосты после упрощения звука)
- CLAUDE.md, раздел 3: схема цикла обновлена с 7 этапов на актуальные 11 (по
  списку STAGES в `scripts/loop.py`). Убрана приписка-костыль под таблицей, вместо
  неё — указание, что источник истины по этапам — `loop.py`. Правило «05-qa не
  пропускать» сохранено. В разделе 5 «семь этапов» → «одиннадцать».
- Хвосты после упрощения `audio_clean.py`: workflow'ы падали на удалённых ключах.
  `audio-test.yml` (`--compare`) и `audio-fix.yml` (`--audiofix`) — ключи убраны,
  оба теперь гоняют обычный режим (шумодав + громкость) и отдают один готовый файл
  `clip_audio_clean.mp4`. Устаревшие имена артефактов (compare_*, audiofix_*) и
  комментарий про 4+1 варианта поправлены.
- ⚠️ После правки `audio-test.yml` и `audio-fix.yml` делают почти одно и то же —
  можно оставить один. Жду решения Анны, сам не удаляю.
- brain/RULES.md: добавлено ПРАВИЛО 11 — всё показываемое пишу только по-русски
  (версия 4 → 5).

### 2026-08-11 (звук — упрощение)
- `audio_clean.py` урезан до ДВУХ фильтров: `afftdn` (мягкий) → `loudnorm`.
  Убрано: highpass, compand, alimiter, деэссер, deboom, резка пауз, режимы
  сравнения/audiofix (425→66 строк по diff). Анна: «ровно две вещи — убрать шум
  и прибавить громкость».
- `presets/audio.json`: `target_lufs -9.3`, `true_peak_db -1`, `denoise gentle`.
  Настроено по эталону https://youtu.be/wXXP9Bzh5bg (−9.3 LUFS, пик 0.1, 4–8к −31).
- Прогон на 30-сек отрезке: −15.5 → −9.7 LUFS, яркость −41.5 → −37 dB. Отправлен
  один файл Анне (без A/B). Открыто: яркость эталона −31 из двух фильтров недостижима
  (потолок записи −35.7), до −31 нужен третий шаг — подъём верха; ждём решение Анны.

### 2026-08-11
- Субтитры 03b: добавлена караоке-подсветка звучащего слова (жёлтым). Принято.
- Исправлен порядок этапов: Vizard-нарезка (03a-cut, бывший 02-cut) перемещена
  ПОСЛЕ обработки (звук/стаб/цвет), субтитры — после нарезки. Vizard-субтитры off.
- Геометрия субтитров пересчитана под 9:16 (размер от меньшей стороны).
- Дубли: авто-резка выключена (`cut: off`) — режет Анна вручную.
- Создан этот HANDOVER.md; в CLAUDE.md добавлено правило читать/дописывать его.

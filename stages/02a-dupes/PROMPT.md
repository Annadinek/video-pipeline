# ЭТАП 02a — ПОИСК ДУБЛЕЙ
Роль: Режиссёр (brain/ROLES.md)

Стоит ПЕРЕД звуком и цветом: дубли ищем до обработки, чтобы не красить и не
стабилизировать то, что потом вырежем. Порядок:
02-cut → **02a-dupes** → 02b-audio → 02c-stab → 03-color.

## ЧТО ЧИТАЮ
- outputs/ready/[id]/clip.mp4  (уже скачанный файл; этап сам ничего не качает)
- presets/dupes.json (настройки)

## ЧТО ДЕЛАЮ
Запускаю `scripts/find_dupes.py --input outputs/ready/[id]/clip.mp4`:
1. Распознаю речь через Whisper (faster-whisper) с метками времени.
2. Сохраняю расшифровку в ДВА файла — переиспользуем для субтитров,
   второй раз не распознаём:
   - outputs/ready/[id]/transcript.srt (с метками)
   - outputs/ready/[id]/transcript.txt (сплошным текстом)
3. Ищу дубли: соседние куски с похожим текстом.
4. ПОКА НЕ РЕЖУ. Только показываю список — Анна проверяет.

## КАК ОТЛИЧАЮ ДУБЛЬ ОТ ПРИЁМА
- Дубль — одна и та же фраза целиком, подряд, короткий разрыв.
- Приём — совпадает только начало, концовка расходится
  («снова те же мысли» / «снова те же люди»).
- Значит: сравниваю фразу ЦЕЛИКОМ (Левенштейн по всей строке) И требую,
  чтобы совпали КОНЦОВКИ (последнее слово). У приёма концовки разные — отсеян.
- Сомневаюсь — не считаю дублем. Лучше пропустить, чем вырезать живое.

## НАСТРОЙКИ (presets/dupes.json)
- similarity_threshold 0.75 — совпадение текста от 75%
- max_gap_sec 1.5 — разрыв между повторами не больше 1.5 с
- min_words 3 — кусок короче не считаем
- end_word_ratio 0.8 — насколько совпадают концовки
- fillers — слова-паразиты, которые чищу перед сравнением
  (ну, это, вот, как бы, значит)
- model / compute_type / language / vad_filter — параметры Whisper

Сравнение: нижний регистр, ё→е, без пунктуации, без паразитов, затем
расстояние Левенштейна. Лемматизации нет — намеренно (тяжело, не окупается).

## ЧТО ОТДАЮ
- outputs/ready/[id]/dupes.json (для машины)
- outputs/ready/[id]/dupes_report.txt (для человека): время начала/конца,
  текст 1-й версии, текст 2-й, похожесть в %
- запись в state/pipeline.json (current.dupes): dupes_found, cut=false,
  имена файлов расшифровки

## КОГДА СТОП
- нет ffmpeg или faster-whisper → blocked
- пустая расшифровка (речь не распознана) → blocked
- две неудачные попытки → blocked, отчёт, идти дальше

## ВАЖНО
Этот этап НЕ режет. Резка — отдельный шаг, только после того как Анна
посмотрит список и подтвердит.
